export interface ISubCategories {
  id: number;
  cat_id: number;
  subcat_id: number;
  subcat_name_bn: string;
  subcat_name_en: string;
  no_of_dua: number;

  createdAt: Date;
  updatedAt: Date;
}
